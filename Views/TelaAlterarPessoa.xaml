<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:appClassePessoaBD.ViewModels"
             x:Class="appClassePessoaBD.Views.TelaAlterarPessoa"
             BackgroundImageSource="fundo.png"
             x:DataType="viewmodels:AlterarPessoaViewModel"
             Title="Alterar Pessoa">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Salvar" IconImageSource="salvarpessoa.png" Command="{Binding SalvarCommand}" />
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <ScrollView>
            <StackLayout Padding="20" Spacing="20">
                <Label Text="Editar Dados" FontSize="18" FontAttributes="Bold" HorizontalOptions="Center" />

                <Border Stroke="{StaticResource ContainerBorder}" BackgroundColor="{AppThemeBinding Light=#40FFFFFF, Dark=#40000000}" Padding="10,5" StrokeShape="RoundRectangle 8">
                    <Entry Placeholder="Nome:"
                           Text="{Binding Nome}"
                           FontAttributes="Bold"
                           ClearButtonVisibility="WhileEditing"
                           TextTransform="Uppercase" />
                </Border>

                <Border Stroke="{StaticResource ContainerBorder}" BackgroundColor="{AppThemeBinding Light=#40FFFFFF, Dark=#40000000}" Padding="10,5" StrokeShape="RoundRectangle 8">
                    <Entry Placeholder="Idade:"
                           Text="{Binding Idade}"
                           FontAttributes="Bold"
                           Keyboard="Numeric"
                           ClearButtonVisibility="WhileEditing" />
                </Border>

                <!-- Label para mostrar erros de validação -->
                <Label Text="{Binding MensagemErro}"
                       TextColor="Red"
                       FontAttributes="Bold"
                       HorizontalOptions="Center" />
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>

</ContentPage>
